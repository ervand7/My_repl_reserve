# Задание 1. Дан список с визитами по городам и странам. Напишите код, который возвращает отфильтрованный список geo_logs, содержащий только визиты из России.

# в итоге должен получиться список
# создай новый отфильтрованный список, в который потом будем добавлять модифицированный исходный список
# запускай цикл for для визитов (так переменную назовем) в исходном списке
# поскольку нам нужно некоторые совпадающие с чем-то элементы, сразу под этим for запускай условную конструкцию if. Условие такое ставь: если список значений визитов с индексами 0 и 1  == 'Россия', то добавляй в новый пустой список созданную в цикле for нашу переменную (визиты)
# печатай получившийся список

geo_logs = [
    {'visit1': ['Москва', 'Россия']},
    {'visit2': ['Дели', 'Индия']},
    {'visit3': ['Владимир', 'Россия']},
    {'visit4': ['Лиссабон', 'Португалия']},
    {'visit5': ['Париж', 'Франция']},
    {'visit6': ['Лиссабон', 'Португалия']},
    {'visit7': ['Тула', 'Россия']},
    {'visit8': ['Тула', 'Россия']},
    {'visit9': ['Курск', 'Россия']},
    {'visit10': ['Архангельск', 'Россия']}
]

geo_logs_filter = []
for visits in geo_logs:
  if list(visits.values())[0][1] == 'Россия':
    geo_logs_filter.append(visits)
print(list(geo_logs_filter))


# Мой позже усовершенствованный вариант

f = []
for i in geo_logs:
  if 'Россия' in list(i.values())[0][1]:
    f.append(i)
a = (list(f))
for s in a:
    for t in s:
      print()
      print(t)
      for dd in (list(s.values())[0]):
        print(dd, end = ' ')
    print()
